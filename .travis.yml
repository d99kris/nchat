language:
  - cpp

sudo:
  - false

os:
  - osx
#  - linux

dist:
  - xenial

addons:
  apt:
    packages:
      - cmake
      - gperf
      - libreadline-dev
      - libssl-dev
      - libncurses-dev
  homebrew:
    packages:
      - gperf
      - cmake
      - openssl
      - ncurses

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make

